HESI A2 TRAINER — FIXED VERSION

Upload these four files together to the root of your GitHub repository:
- index.html
- style.css
- app.js
- questions.json

Important:
1. Do not upload only the ZIP. Unzip it first, then upload the four files.
2. Keep all four files in the same folder.
3. In GitHub Pages, deploy from the main branch and the root folder.
4. After uploading, refresh the website. On iPhone, closing and reopening the tab can clear an old cached version.

Main repair:
The old JavaScript read q.choices while questions.json used q.options. This version accepts either field and includes visible loading errors.
